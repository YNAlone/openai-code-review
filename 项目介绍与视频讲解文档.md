# OpenAi Code Review — 基于 Agentic Coding 的自动化代码审查系统

## 一、项目简介

OpenAi Code Review 是一个基于 **Agentic Coding（智能体编程）** 理念构建的自动化代码审查 SDK。它深度嵌入 CI/CD 流水线（GitHub Actions），在开发者每次提交代码时自动触发，通过大语言模型（ChatGLM）对代码变更进行智能评审，并将评审报告自动存档、通过微信模板消息实时推送给团队成员。

**核心价值**：将 AI 智能体嵌入软件开发质量保障环节，实现"提交即审查、审查即反馈、反馈即归档"的全自动化闭环，显著降低人工 Code Review 成本，提升代码质量和团队协作效率。

**项目定位**：企业级轻量 SDK，Maven 多模块结构，可无缝集成到任何 Java 技术栈的 GitHub 仓库。

---

## 二、产品功能与场景说明

### 2.1 业务场景

在团队协作开发中，Code Review 是保障代码质量的关键环节，但传统人工审查存在以下痛点：

- **时效性差**：提交后等待审查人空闲，往往延迟数小时甚至数天
- **标准不一**：不同审查人的关注点、严格程度差异大
- **易遗漏**：人工审查受疲劳和经验限制，难以覆盖所有潜在问题
- **缺乏记录**：审查结论散落在 PR 评论区，缺乏系统沉淀

OpenAi Code Review 针对以上痛点，通过 AI 智能体实现：

### 2.2 核心功能

| 功能模块 | 描述 |
|---------|------|
| **自动提取代码变更** | 通过 Git 命令获取最新 commit 的 diff，识别新增、修改、删除的代码 |
| **AI 智能代码审查** | 调用 ChatGLM 大模型（glm-4-flash），进行代码深度评审 |
| **评审报告自动归档** | 将 Markdown 格式的审查结果推送至 GitHub 专用仓库，按日期归档 |
| **即时消息通知** | 通过微信公众号模板消息，向团队推送审查完成通知及报告链接 |

### 2.3 评审覆盖维度

AI 智能体会从以下多个维度对代码变更进行全面审查：

- **性能瓶颈**：低效算法、不必要的循环、资源浪费
- **安全隐患**：SQL 注入风险、敏感信息硬编码、权限校验缺失
- **逻辑缺陷**：边界条件遗漏、空指针风险、异常处理缺失
- **可维护性**：命名规范、代码结构、注释完整性
- **资源管理**：连接泄漏、流未关闭、内存泄漏风险

---

## 三、核心架构与智能体设计

### 3.1 架构概览

```
┌─────────────────────────────────────────────────────┐
│                   GitHub Actions                     │
│  (push 事件触发 → 启动 SDK Jar 包)                    │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│              OpenAiCodeReview (入口)                  │
│  加载环境变量配置 → 组装智能体 → 启动执行链            │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│      AbstractOpenAiCodeReviewService (模板方法)       │
│                                                     │
│  exec() 流程：                                       │
│  ① getDiffCode()  →  提取代码变更                    │
│  ② codeReview()   →  AI 智能审查                     │
│  ③ recordCodeReview() → 归档报告                     │
│  ④ pushMessage()  →  即时通知                        │
└─────────────────────────────────────────────────────┘
         │              │              │
         ▼              ▼              ▼
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │GitCommand│  │  ChatGLM  │  │  WeiXin  │
   │(Git操作)  │  │(AI 大模型)│  │(微信推送) │
   └──────────┘  └──────────┘  └──────────┘
```

### 3.2 智能体角色与职责划分

项目采用了**多智能体协同**的设计思想，每个组件都是具有明确职责的智能体：

| 智能体角色 | 对应组件 | 职责 |
|-----------|---------|------|
| **协调智能体** | `AbstractOpenAiCodeReviewService` | 编排整体流程，协调各智能体之间的数据传递与执行顺序 |
| **感知智能体** | `GitCommand` | 从 Git 仓库中感知并提取代码变更信息（git diff） |
| **认知智能体** | `ChatGLM`（IOpenAI 实现） | 理解代码变更意图，运用编程知识库进行审查分析 |
| **执行智能体** | `GitCommand.commitAndPush()` | 将审查结果持久化归档到 GitHub 仓库 |
| **通知智能体** | `WeiXin` | 通过微信模板消息将结果即时触达团队成员 |

### 3.3 智能体协同机制

```
感知智能体               认知智能体               执行智能体 + 通知智能体
(GitCommand)            (ChatGLM)                (Git + WeiXin)
    │                       │                         │
    │ ① git diff           │                         │
    │   (提取变更)          │                         │
    │                       │                         │
    ├──── diffCode ────────►│                         │
    │                       │ ② 构造 Prompt           │
    │                       │   (角色设定+评审指令)     │
    │                       │ ③ 调用 ChatGLM API      │
    │                       │ ④ 返回评审报告           │
    │                       │                         │
    │                       ├──── recommend ─────────►│
    │                       │                         │ ⑤ clone仓库→写入报告
    │                       │                         │ ⑥ git commit & push
    │                       │                         │ ⑦ 获取微信access_token
    │                       │                         │ ⑧ 发送模板消息(含报告链接)
    │                       │                         │
    │                       │                         ├── logUrl ──► 团队微信
```

**关键设计原则**：

- **单向数据流**：感知 → 认知 → 执行/通知，职责清晰，低耦合
- **接口抽象**：`IOpenAI` 接口使得认知智能体可替换为任何大模型（ChatGPT、Claude 等）
- **模板方法模式**：`AbstractOpenAiCodeReviewService` 定义执行骨架，子类实现具体步骤，便于扩展

---

## 四、使用方法

### 4.1 快速接入（GitHub Actions 方式）

在目标仓库创建 `.github/workflows/code-review.yml`：

```yaml
name: OpenAi Code Review
on:
  push:
    branches: [master, main, develop]

jobs:
  code-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 2          # 获取最近两次提交以生成 diff
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'adopt'

      - name: Download & Run Review SDK
        env:
          # ===== Git 配置 =====
          GITHUB_REVIEW_LOG_URI: "https://github.com/xxx/openai-code-review-log"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMIT_PROJECT: ${{ github.event.repository.name }}
          COMMIT_BRANCH: ${{ github.ref_name }}
          COMMIT_AUTHOR: ${{ github.actor }}
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          # ===== ChatGLM 配置 =====
          CHATGLM_APIHOST: "https://open.bigmodel.cn/api/paas/v4/chat/completions"
          CHATGLM_APIKEYSECRET: ${{ secrets.CHATGLM_APIKEYSECRET }}
          # ===== 微信配置 =====
          WEIXIN_APPID: ${{ secrets.WEIXIN_APPID }}
          WEIXIN_SECRET: ${{ secrets.WEIXIN_SECRET }}
          WEIXIN_TOUSER: ${{ secrets.WEIXIN_TOUSER }}
          WEIXIN_TEMPLATE_ID: ${{ secrets.WEIXIN_TEMPLATE_ID }}
        run: |
          wget -O openai-code-review-sdk.jar ${{ secrets.SDK_DOWNLOAD_URL }}
          java -jar openai-code-review-sdk.jar
```

### 4.2 本地测试方式

```bash
# 设置环境变量
export GITHUB_REVIEW_LOG_URI="https://github.com/xxx/openai-code-review-log"
export GITHUB_TOKEN="ghp_xxxxxxxxxxxxx"
export COMMIT_PROJECT="test-project"
export COMMIT_BRANCH="master"
export COMMIT_AUTHOR="developer"
export COMMIT_MESSAGE="feat: add new feature"
export CHATGLM_APIHOST="https://open.bigmodel.cn/api/paas/v4/chat/completions"
export CHATGLM_APIKEYSECRET="xxx.yyy"
export WEIXIN_APPID="wxXXXX"
export WEIXIN_SECRET="xxxx"
export WEIXIN_TOUSER="xxxx"
export WEIXIN_TEMPLATE_ID="xxxx"

# 编译运行
mvn clean package
java -jar openai-code-review-sdk/target/openai-code-review-sdk.jar
```

---

## 五、与 AI 知识的深度结合

### 5.1 Prompt Engineering（提示词工程）

项目中的核心 AI 能力体现在精心的 Prompt 设计上（见 `OpenAiCodeReviewService.java:37-65`）。Prompt 设计包含以下关键要素：

**角色设定（Role Prompting）**：

> "你是一位资深编程专家，拥有深厚的编程基础和广泛的技术栈知识..."

通过明确的角色定位，让大模型"戴上"资深编程专家的面具，输出更专业的审查意见。

**结构化输出约束（Structured Output）**：
> "返回格式严格如下：`# OpenAi 代码评审.` `### 代码评分：{变量1}` `#### 代码逻辑与目的：{变量6}` ..."

通过模板约束输出格式，确保：
- 评分可量化（0~100分）
- 问题点分类明确（性能、安全、规范等）
- 修改建议有可操作性（包含修改后的代码）

**多维度审查指令（Chain of Thought）**：
> "可能的性能瓶颈、逻辑缺陷、潜在问题、安全风险、命名规范、注释、以及代码结构、异常情况、边界条件、资源的分配与释放等等"

引导模型从多个维度逐一审查，形成链式思考，避免遗漏。

### 5.2 大模型 API 集成与鉴权

- **JWT Bearer Token 鉴权**（`BearerTokenUtils.java`）：基于智谱 AI 的 API Key + Secret 签名机制，生成有时效的 JWT Token
- **本地缓存优化**（Guava Cache）：缓存 Token，过期前复用，减少不必要的签名计算和网络请求
- **同步调用方式**：适用于代码审查这种非实时交互场景，在 GitHub Actions 中稳定可靠

### 5.3 Agentic Coding 设计模式

| 模式 | 实现 | 说明 |
|------|------|------|
| **模板方法模式** | `AbstractOpenAiCodeReviewService` | 定义智能体执行骨架，子类可定制各步骤策略 |
| **策略模式** | `IOpenAI` 接口 | AI 后端可灵活替换（ChatGLM/GPT/Claude） |
| **协调者模式** | `OpenAiCodeReviewService.exec()` | 统一编排多智能体交互流程 |

### 5.4 Git 底层原理应用

`GitCommand.java` 中体现的 Git 知识：

- **哈希链式结构**：通过 `git log -1 --pretty=format:%H` 获取最新 commit 哈希
- **Myers 差异算法**：通过 `git diff commitHash^ commitHash` 获取父子提交的内容差异
- **分支管理**：评审报告推送到独立的日志仓库分支，实现"评审结果与源码分离"



---

## 六、技术栈总结

| 技术 | 用途 |
|------|------|
| Java 8 | 主要开发语言 |
| Spring Boot 2.7.12 | 项目骨架依赖管理 |
| Maven 多模块 | 项目构建与模块管理 |
| ChatGLM API（智谱AI） | AI 大模型代码审查 |
| JGit | Git 仓库操作（clone/commit/push） |
| JWT (auth0) | API Key JWT 签名鉴权 |
| Guava Cache | Token 本地缓存 |
| FastJSON2 | JSON 序列化/反序列化 |
| 微信公众平台 API | 模板消息推送通知 |
| GitHub Actions | CI/CD 自动触发与执行 |

---

## 七、项目亮点

1. **完整的闭环自动化**：从代码提交到审查反馈全自动，零人工干预
2. **多智能体架构设计**：感知、认知、执行、通知四个角色的智能体分工明确、协同高效
3. **深入 Prompt 工程**：角色设定 + 结构化输出 + 多维度审查指令，确保审查质量
4. **企业级代码质量**：DDD 分层架构、模板方法模式、接口抽象，具备良好的可扩展性
5. **即时触达能力**：微信模板消息让团队成员第一时间获取审查结果，形成快速反馈闭环
